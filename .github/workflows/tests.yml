name: Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Set up Julia
      uses: julia-actions/setup-julia@v2
      with:
        version: '1.9'

    - name: Install Julia dependencies
      run: |
        julia -e 'using Pkg; Pkg.add(["SparseArrays", "LinearAlgebra", "JSON", "IntegerSmithNormalForm", "AbstractAlgebra", "Statistics", "Combinatorics", "PrecompileTools", "Random", "Graphs", "SimpleWeightedGraphs", "DelaunayTriangulation"])'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install .[test,all]
        
    - name: Test with pytest
      run: |
        pytest tests/
